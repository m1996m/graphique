{% extends 'base.html.twig' %}

{% block title %}Audiometrie{% endblock %}

{% block body %}
    <h1>Audiometrie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ audiometrie.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ audiometrie.designation }}</td>
            </tr>
            <tr>
                <th>Typeaudiometrie</th>
                <td>{{ audiometrie.typeaudiometrie }}</td>
            </tr>
            <tr>
                <th>Typeoreille</th>
                <td>{{ audiometrie.typeoreille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_audiometrie_index') }}">back to list</a>

    <a href="{{ path('app_audiometrie_edit', {'id': audiometrie.id}) }}">edit</a>

    {{ include('audiometrie/_delete_form.html.twig') }}
{% endblock %}
