<form method="post" action="{{ path('app_audiometrie_delete', {'id': audiometrie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ audiometrie.id) }}">
    <button class="btn">Delete</button>
</form>
